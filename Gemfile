# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# dev
gem 'awesome_print'

# core
gem 'dotenv'
gem 'discordrb'
gem 'sequel'
gem 'sqlite3'

# helpers (used in stock)
gem 'actionview', require: 'action_view'

# command: lastfm
gem 'lastfm'
gem 'rspotify', github: 'benphelps/rspotify', branch: 'rest-client-version'

# command: weather
gem 'forecast_io'
gem 'geocoder'

# command: stock
gem 'iex-ruby-client'

# command: mtg
gem 'scryfall', require: 'scryfall/cards'
